#foreign fn printf!(u8* format) : i32 {}
#foreign fn fopen(u8* path,u8* mode) : u64* {}

struct Position
{
   i32 x;
   i32 y;
};

struct Player
{
   Position pos;
};

fn main() : i32 {
   Player player;
   player.pos.x;
   // player.pos -> __tmp3 = ((u64) & ((Player *)__tmp2)->pos);
   // .pos.x -> __tmp4 = ((u64) & ((Position *)__tmp3)->x);
   // return __tmp4
}