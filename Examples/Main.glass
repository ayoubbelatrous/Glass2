#foreign fn printf!(u8* format) : i32 {};
#foreign fn malloc(i32 size) : void* {};
#foreign fn free(void* ptr) : void {};

struct vec3 {
	f32 x;
	f32 y;
	f32 z;
};

fn main() : i32 {

	bool variable_a = 1;
	bool variable_b = 0-1;

	if variable_a {
		printf("Hello World\n\n\n");
	}

	return 0;
}