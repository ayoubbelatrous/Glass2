#foreign fn printf!(u8* format) : i32 {};
#foreign fn malloc(i32 size) : void* {};
#foreign fn free(void* ptr) : void {};

#foreign fn strlen(u8* str) : u64 {};

#foreign fn rand() : i32 {};

struct Entity
{
	void* Age2;
	i32 Age;
	i32 Age1;
	i32 Age1;
};

fn take_array(Any* array) : i32 {
	return 0;
}

fn main() : i32 {
	return 0;
}