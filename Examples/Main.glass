#foreign fn printf!(u8* format) : i32 {};
#foreign fn malloc(i32 size) : void* {};
#foreign fn free(void* ptr) : void {};

struct Transform {
	i32 component;
};

struct Entity {
	Transform* ts;
};

fn main() : i32 {
	i32* variable = cast(i32*) malloc(sizeof(i32));

	*variable = 30;

	printf("ptr: %p", variable);

	return *variable;
}