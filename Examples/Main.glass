#foreign fn printf!(u8* format) : i32 {};
#foreign fn malloc(i32 size) : i32* {};

struct Transform {
	i32 component;
};

struct Entity {
	Transform* ts;
};

fn main() : i32 {
	i32* array = malloc(10);
	array[0] = 30;

	printf("Hello World, %i\n", sizeof(i32));

	return array[0];
}